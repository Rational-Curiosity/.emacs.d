;;; vlf-config.el --- Configure very large files

;;; Commentary:

;; Usage:
;; (require 'vlf-config)

;;; Code:

(require 'vlf-setup)

(setq vlf-batch-size 32768)

(provide 'vlf-config)
;;; vlf-config.el ends here
